const OrderStatus = () => {
  return (
    <div className="fixed bottom-16 left-0 right-0 bg-black border-t border-white/10 p-4 max-w-md mx-auto">
      <p className="text-sm text-gray-300 text-center">
        If order shows queue no at bottom, fixed always of order status: wait
      </p>
    </div>
  );
};

export default OrderStatus;